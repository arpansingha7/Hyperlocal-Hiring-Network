# Core Application Settings
# Define the Production Backend URL where your API resides.
# Ensure this matches where app.js is deployed (e.g. Render, Heroku)

VITE_API_BASE_URL=https://your-production-backend.com/api/v1
# VITE_API_BASE_URL=http://localhost:4000/api/v1 # For local development
